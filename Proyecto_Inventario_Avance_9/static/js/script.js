function mostrarAviso(modulo) {
    alert(
        modulo + ": interfaz demostrativa. " +
        "En el Avance 9 todavía no se requiere guardar información en una base de datos."
    );
}

document.addEventListener("DOMContentLoaded", function () {
    console.log("Sistema de Inventario - Flask y Jinja2 cargados correctamente.");
});
